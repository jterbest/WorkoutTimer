#pragma once
#include "timer.h"

/**
 * \brief Create isometric strength training timer
 * 
 * \param timer The timer initialize as a isometric timer.
 * 
 */
void create_isometric_timer(PTimerState timer);
